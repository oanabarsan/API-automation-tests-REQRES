{
  "name": "api-tests-reqres",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "positive-tests": "npx mocha positive_tests --reporter mochawesome --reporter-options reportDir=Results_positive_tests,reportFilename=api-positive_test-results --timeout 10000",
    "negative-tests": "npx mocha negative_tests --reporter mochawesome --reporter-options reportDir=Results_negative_tests,reportFilename=api-negative_test-results --timeout 10000"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "mocha": "^10.2.0",
    "mochawesome": "^7.1.3",
    "npx": "^10.2.2",
    "pactum": "^3.4.0"
  }
}
